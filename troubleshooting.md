# 🔧 Диагностика и решение проблем (S905X3)

Проблема с установкой Armbian на S905X3 часто связана с различиями в ревизиях плат и версиях u-boot, даже если корпус и название модели одинаковы.

## 1. Идентификация железа
Перед установкой выясните точную модель платы:
```bash
cat /proc/device-tree/model
dmesg | grep -i eth
lsusb
